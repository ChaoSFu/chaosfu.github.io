name: Build & Deploy Daily Data

on:
  schedule:
    - cron: '10 7 * * 1-5'   # UTC 07:10 ≈ 北京时间 15:10 工作日
  workflow_dispatch: {}

jobs:
  etl:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install deps
        run: pip install -r stock-analysis/scripts/requirements.txt

      - name: Run ETL (Fetch Real Data from Eastmoney)
        env:
          MODE: "EASTMONEY"  # EASTMONEY=东方财富实时数据(推荐) / MOCK=测试数据 / CSV / API
        run: |
          python stock-analysis/scripts/etl_daily.py \
            --mode "${MODE}" \
            --top-boards 20 \
            --stocks-per-board 10 \
            --out "docs/data/daily.json"

      - name: Archive Today's Data
        run: |
          TODAY=$(date +%Y-%m-%d)
          mkdir -p docs/data/archive
          cp docs/data/daily.json "docs/data/archive/${TODAY}.json"
          echo "✅ Archived today's data to archive/${TODAY}.json"

      - name: Generate History Data
        run: |
          python stock-analysis/scripts/generate_history.py \
            --archive-dir docs/data/archive \
            --days 7 \
            --out docs/data/history.json

      - name: Commit and Push Data to Main Branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/data/
          git diff --staged --quiet || git commit -m "Auto-update stock data $(date +%Y-%m-%d)

          📊 Updated by GitHub Actions
          - Daily data: docs/data/daily.json
          - History data: docs/data/history.json
          - Archive: docs/data/archive/$(date +%Y-%m-%d).json
          "
          git push
